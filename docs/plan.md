
# browser-automation plan

This repository builds an hourly X.com Home digest that uses a browser session to collect posts, ranks them with configurable weights from `.env.local`, and renders a static HTML report.

The full implementation plan and task breakdown live in the local-only Hermes artifacts under `.hermes/`.
